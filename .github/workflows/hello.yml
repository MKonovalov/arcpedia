name: Hello World

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Generate bot token
        id: bot-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ steps.bot-token.outputs.token }}

      - name: Install arc
        run: |
          REPO="yologdev/arc-evolve"
          LATEST=$(curl -fsSL "https://api.github.com/repos/$REPO/releases/latest" | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/')
          echo "Installing arc $LATEST..."
          curl -fsSL "https://github.com/$REPO/releases/download/$LATEST/arc-$LATEST-x86_64-unknown-linux-gnu.tar.gz" -o /tmp/arc.tar.gz
          tar xzf /tmp/arc.tar.gz -C /tmp
          mkdir -p "$HOME/.local/bin"
          cp /tmp/arc "$HOME/.local/bin/arc" || cp /tmp/arc-*/arc "$HOME/.local/bin/arc"
          chmod +x "$HOME/.local/bin/arc"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Verify arc
        run: arc --version

      - name: Test API key
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Say hello in one sentence." | arc --model claude-opus-4-6
